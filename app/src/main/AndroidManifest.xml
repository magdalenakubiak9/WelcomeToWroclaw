<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.welcometowroclaw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SightsActivity"
            android:label="@string/sights_string" />
        <activity
            android:name=".MuseumsActivity"
            android:label="@string/museums_string" />
        <activity
            android:name=".RestaurantsActivity"
            android:label="@string/restaurants_string" />
        <activity
            android:name=".EventsActivity"
            android:label="@string/events_string" />
    </application>

</manifest>